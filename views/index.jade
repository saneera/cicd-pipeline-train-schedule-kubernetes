extends layout

block content
  h1 Find your train!
  h1 UPDATE INDEX FILE
  p Select your train below to see its current schedule. updte index file
  #wrapper
    #trainList
      h2 Trains
      #trains.d-flex.flex-row
    #trainInfo
      strong  <span id='trainName'></span>
      #trainSchedule
        strong Select a train to view its current schedule.
